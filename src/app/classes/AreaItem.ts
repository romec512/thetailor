export class AreaItem {
    // Текущий индекс в массиве
    index: number;
    // Индекс следующего элемента в массиве
    next: AreaItem;
    height: number;
    width: number;
    lastWidth: number;
    number: number;
}
